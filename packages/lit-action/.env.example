# Example environment variables for Lit Action
# Copy this file to .env.development and .env.production and fill in your values

# Telegram Bot API Key (will be encrypted during build)
# Note: Use VITE_ prefix so Vite loads these variables
VITE_TELEGRAM_BOT_API_KEY=your_telegram_bot_api_key_here

# Add other environment variables as needed (use VITE_ prefix)
